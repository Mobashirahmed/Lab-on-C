// Write a program that illustrates the use of both single-line (`//`) and multi-line (`/* ... */`) comments, ensuring code segments inside comments are ignored by the compiler.
#include<stdio.h>
int main() {
    // This is a single-line comment
    /* This is a multi-line comment
       It can span multiple lines */
    printf("Hello, World!\n");
    return 0;
}