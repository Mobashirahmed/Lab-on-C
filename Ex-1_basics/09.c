// Write a program to print special symbolic characters on the console window that are normally interpreted as syntax, specifically the percentage symbol (`%`), backslash (`\`), and double quotes (`"`).
#include<stdio.h>
int main() {
    printf("Percentage symbol: %%\n");
    printf("Backslash: \\\n");
    printf("Double quotes: \"\"\n");
    return 0;
}